import { StyleSheet } from 'react-native';

// Themes
import { BASE_COLORS } from '@/themes';

export const styles = StyleSheet.create({
  wrapper: {
    alignSelf: 'center',
    overflow: 'hidden',
    backgroundColor: BASE_COLORS.lighthouse,
  },
});
